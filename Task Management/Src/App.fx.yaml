App As appinfo:
    BackEnabled: =true
    OnStart: |-
        =Set(varGridEdit,false);
        
        Set(varNewRecord,Defaults('Task Update'));
        
        Set(varNumber,10000000)
    StartScreen: =
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

